# frozen_string_literal: true

RSpec.describe AbiCoderRb do
  it "has a version number" do
    expect(AbiCoderRb::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
